@* @model AttendanceViewModel

@{
    ViewData["Title"] = "Edit Attendance";
}

<h2>Edit Attendance for @Model.StudentFirstName @Model.StudentLastName</h2>
<br />

@if (!ViewData.ModelState.IsValid)
    {
    <div class="alert alert-danger">
        <h4>Please correct the following errors:</h4>
        <ul>
            @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                {
                <li>@error.ErrorMessage</li>
                }
        </ul>
    </div>
    }

<form asp-action="EditAttendance">
    <input type="hidden" asp-for="AttendanceID" />
    <input type="hidden" asp-for="FacultyID" />
    <input type="hidden" asp-for="SubjectName" />

    <div class="form-group">
        <label asp-for="StudentFirstName" class="control-label">Student Name:</label>
        <input type="text" asp-for="StudentFirstName" class="form-control" readonly />
    </div>

    <div class="form-group">
        <label asp-for="Status" class="control-label">Status:</label>
        <div class="form-check">
            <input type="checkbox"  asp-for="Status" class="form-check-input" />
            <label class="form-check-label">Present</label>
        </div>
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="AbsentStudents" asp-route-subjectName="@Model.SubjectName" asp-route-facultyId="@Model.FacultyID" class="btn btn-secondary">Cancel</a>
</form>



   
 *@
@model AttendanceViewModel

@{
    ViewData["Title"] = "Edit Attendance";
}

<h2>Edit Attendance for @Model.StudentFirstName @Model.StudentLastName</h2>
<br />

@if (!ViewData.ModelState.IsValid)
    {
    <div class="alert alert-danger">
        <h4>Please correct the following errors:</h4>
        <ul>
            @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                {
                <li>@error.ErrorMessage</li>
                }
        </ul>
    </div>
    }

<form asp-action="EditAttendance">
    <input type="hidden" asp-for="AttendanceID" />
    <input type="hidden" asp-for="FacultyID" />
    <input type="hidden" asp-for="SubjectName" />

    <div class="form-group">
        <label asp-for="StudentFirstName" class="control-label">Student Name:</label>
        <input type="text" asp-for="StudentFirstName" class="form-control" readonly />
    </div>

    <div class="form-group">
        <label asp-for="Status" class="control-label">Status:</label>
        <div class="form-check">
            <input type="checkbox" asp-for="Status" class="form-check-input" />
            <label class="form-check-label">Present</label>
        </div>
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="AbsentStudents" asp-route-subjectName="@Model.SubjectName" asp-route-facultyId="@Model.FacultyID" class="btn btn-secondary">Cancel</a>
</form>
